# A simple sandbox image that just prints a message and exits.
# This is to fulfill the docker-compose setup where the sandbox
# container is expected to exit successfully.
FROM alpine:latest

# Run a simple command
CMD echo "Sandbox image is ready. Container will now exit."
